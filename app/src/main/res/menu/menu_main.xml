<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:thisApp="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/item_refresh"
        android:title="Refresh"
        android:showAsAction="always"/>

    <item android:id="@+id/item_commitChanges"
        android:title="Commit changes"
        android:showAsAction="always"
        android:enabled="false"/>

    <item android:id="@+id/item_more"
        android:title="More"
        android:showAsAction="always">
        <menu>
            <item android:id="@+id/item_onlyExistingFrames"
                android:title="Only Existing Frames"
                android:showAsAction="never"/>
        </menu>
    </item>

</menu>
